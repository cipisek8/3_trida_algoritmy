<Window x:Class="Spirala.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Spirala"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">
    <Grid Margin="50,50,50,50" x:Name="Field">
        <Grid x:Name="Menus">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" x:Name="LengthInput" Text="Starting length" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="GapInput" Text="Gap" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <CheckBox Grid.Column="2" x:Name="RecursionInput" Content="Use Recursion" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Grid.Column="3" Content="Render" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Render"/>
        </Grid>
    </Grid>
</Window>
